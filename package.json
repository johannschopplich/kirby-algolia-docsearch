{
  "type": "module",
  "version": "0.5.8",
  "private": true,
  "packageManager": "pnpm@9.0.6",
  "scripts": {
    "release": "pnpm run build && bumpp --all --execute \"npx zx scripts/pre-release.mjs\""
  },
  "devDependencies": {
    "bumpp": "^9.4.0",
    "zx": "^8.0.2"
  }
}
